#posting app

#description
users can sign up and then signin so and they have an one hour session then expired 
and they can use this app as a platform to post about anything and upload an image for it and 
they able to edit or delete only the post which created by them .

#Run 
to run backend run npm install then npm run start
to run frontend run npm install then ng serve -o
regarding the database Iam deploying a cluser so any change will affect it.